{
  "id": "string (stable sample id)",
  "task": "suggest_blank | suggest_selection | suggest_sentence | rewrite | lexical | constraints | oneword",
  "input": {
    "sentence": "string (editor tasks)",
    "context": "string",
    "mode": "write | edit | rewrite",
    "selection": {
      "text": "string",
      "start": "int",
      "end": "int"
    },
    "trigger": "auto | button",
    "word": "string (lexical tasks)",
    "lexical_task": "synonyms | antonyms | rhymes | homonyms",
    "rhyme_with": "string (constraints)",
    "relation": "synonym | antonym",
    "meaning_of": "string (constraints)",
    "query": "string (one-word substitution)"
  },
  "expected": {
    "positives": ["ordered highest-quality expected outputs"],
    "acceptable": ["optional weaker-but-valid outputs"],
    "negatives": ["optional known-bad outputs"]
  },
  "input_text": "string (generated by build_dataset.py)",
  "candidates": [
    {
      "text": "string",
      "label": "0..3",
      "model_score": "float",
      "pos": "optional POS tag",
      "reason": "string",
      "source": "model source"
    }
  ],
  "stats": {
    "candidate_count": "int",
    "positive_count": "int"
  },
  "note": "optional generation note"
}

