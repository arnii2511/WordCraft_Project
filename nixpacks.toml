[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -r backend/requirements.txt"
]

[start]
cmd = "uvicorn backend.main_ml:app --host 0.0.0.0 --port $PORT"
